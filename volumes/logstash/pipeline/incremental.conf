input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.22.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://ls-eb98b9ab4c0631279a64c7f931fea25b9dc4c3d6.coosir2txuvl.ap-south-1.rds.amazonaws.com:3306/mlsdb?allowMultiQueries=true"
    jdbc_user => "dbmasteruser"
    jdbc_password => "|[#(A,0Aj:##6GzKW;]nz6(2a<=G<H|d"
    statement_filepath => "/usr/share/logstash/config/queries/incremental.sql"
    use_column_value => true
    tracking_column => "last_updated_date"
    tracking_column_type => "timestamp"
    schedule => "/20 * * * *"
  }
}
filter {
    json {
	source => "artists"
    }
    json {
	source => "categories"
    }
    json {
	source => "dates"
    }
    json {
	source => "events"
    }
    json {
	source => "languages"
    }
    json {
	source => "locations"
    }
    json {
	source => "projects"
    }
    json {
	source => "tags"
    }
    mutate {
        remove_field => ["@version", "@timestamp", "action_type"]
    }
}
output {
  # stdout { codec => rubydebug { metadata => true } }
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "tags"
    action => "index"
    document_id => "%{item_id}"
  }
}
